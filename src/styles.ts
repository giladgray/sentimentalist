import { Colors } from "@blueprintjs/core";

import * as csstips from "csstips";
export { csstips }

export { style } from "typestyle";

export const theme = {
  /** background color styles */
  background: { background: Colors.DARK_GRAY3 },
  /** size of a grid unit, great for multiplying */
  gridSize: 10,
};
